<% content_for :title, "Edit Session - Admin" %>

<div class="mb-6">
  <h1 class="text-2xl font-bold text-gray-900">Edit Knowledge Session</h1>
  <p class="text-gray-600"><%= @session.code %> - <%= @session.title.truncate(60) %></p>
</div>

<%= render "form", session: @session %>
